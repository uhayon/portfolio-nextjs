import React, { FC, HTMLAttributes } from "react";
import cx from "clsx";

export interface {{pascalCase name}}Props extends Omit<HTMLAttributes<HTMLDivElement>, "className"> {
  /** Define component props here */
}

const {{pascalCase name}}: FC<{{pascalCase name}}Props> = () => {
  const className = cx("{{lowerCase name}}");

  return (
    <div>
      <h1 className={className}>{{pascalCase name}}</h1>
    </div>
  );
};

export default {{pascalCase name}};
